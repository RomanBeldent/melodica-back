{% extends 'base.html.twig' %}

{% block title %}Edit Band{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between m-3">
		<h2>Mettre à jour le groupe {{ band.name }} </h2>
		<a href="{{ path('back_band_list') }}" class='mb-3 btn btn-secondary'>Retour</a>
	</div>
    {{ include('partials/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('band/_delete_form.html.twig') }}
{% endblock %}
