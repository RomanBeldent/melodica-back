{% extends 'base.html.twig' %}

{% block title %}New Band{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between m-3">
		<h2>Création d'un nouveau groupe</h2>
		<a href="{{ path('back_band_list') }}" class='btn btn-secondary'>Retour</a>
	</div>

	{{ include('partials/_form.html.twig') }}

{% endblock %}
