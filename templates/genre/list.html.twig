{% extends 'base.html.twig' %}

{% block title %}Liste des genres
{% endblock %}

{% block body %}
	<h1>Liste des genres</h1>

	<table class="table table-dark table-striped">
		<a href="{{ path('back_genre_create')}}" class="btn btn-secondary btn-lg active " role="button" aria-pressed="true">Ajouter un nouveau genre</a>
		<thead>
			<tr>
				<th>Id</th>
				<th>Name</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for genre in genres %}
				<tr>
					<td>{{ genre.id }}</td>
					<td>{{ genre.name }}</td>
					<td>
						<a href="{{ path('back_genre_show', {'id': genre.id}) }}" class="mr-3 btn btn-outline-info">Read
							<a href="{{ path('back_genre_edit', {'id': genre.id}) }}" class="mr-3 btn bts-sm btn-warning">Update
								<a href="{{ path('back_genre_delete', {'id': genre.id}) }}" class="mr-3 btn btn-outline-danger">Delete</td>
							</td>
						</tr>
					{% else %}
						<tr>
							<td colspan="3">no records found</td>
						</tr>
					{% endfor %}
				</tr>
			</tbody>
		</tbody>
	</table>
{% endblock %}
