{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between m-3">
		<h2>Type
			{{ type.name }}
		</h2>
		<a href="{{ path('back_type_list') }}" class='mb-3 btn btn-secondary'>Retour</a>
		</div>
		<table class="table table-dark table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_type_edit', {'id': type.id}) }}" class='mb-3 btn btn-warning'>Mettre à jour</a>
    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
