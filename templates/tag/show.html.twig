{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between m-3">
		<h2>Tag
			{{ tag.name }}
		</h2>
		<a href="{{ path('back_tag_list') }}" class='mb-3 btn btn-secondary'>Retour</a>
		</div>
		<table class="table table-dark table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('back_tag_edit', {'id': tag.id}) }}" class='mb-3 btn btn-warning'>Mettre à jour</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
