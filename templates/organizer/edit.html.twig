{% extends 'base.html.twig' %}

{% block title %}Modification organisateur
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between m-3">
		<h2>Mettre à jour l'organisateur {{ organizer.name }}</h2>
		<a href="{{ path('back_organizer_list') }}" class='mb-3 btn btn-secondary'>Retour</a>
	</div>
	{{ include('partials/_form.html.twig', {'button_label': 'Update'}) }}

	{{ include('organizer/_delete_form.html.twig') }}
{% endblock %}
